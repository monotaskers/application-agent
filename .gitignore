# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# logs
*.log
*.log.*
**/*.log

# env files (can opt-in for committing if needed)
.env*
# check in example files
!.env.example
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.mastra/

# lock files
package-lock.json
yarn.lock
# pnpm-lock.yaml - committed for Vercel builds
bun.lockb

# python
agent/venv/
agent/__pycache__/
agent/.venv/

# Supabase local development
# Keep migrations/ and config.toml in git, but ignore temp files
supabase/.branches/
supabase/.temp/
supabase/.seed/
supabase/.logs/

/tmp
/_local

# http://localhost:3000/api/jwt to get the JWT token for backend authentication
# We do not want to commit to the repo, used for local development only.
src/app/api/jwt/

# Storybook
.storybook-static/
storybook-static/