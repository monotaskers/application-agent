# =============================================================================
# Environment Variables Template
# =============================================================================
# 
# Copy this file to .env.local and fill in your actual values.
# DO NOT commit .env.local or any file with real credentials to git.
#
# Usage:
#   cp .env.example .env.local
#   # Then edit .env.local with your actual values
#
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Authentication (REQUIRED)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Required for admin operations (server-side only)
# Get from: Supabase Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# Application URL (OPTIONAL - Required for Production/Vercel)
# -----------------------------------------------------------------------------
# Set this to your production URL to fix auth redirects in production.
# For local development, this can be omitted (will use window.location.origin)
# For Vercel deployments, set to: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# CopilotKit Configuration (OPTIONAL - Has Defaults)
# -----------------------------------------------------------------------------
# CopilotKit agent backend URL
# Default: http://localhost:8000/
COPILOTKIT_AGENT_URL=http://localhost:8000/

# CopilotKit agent identifier
# Default: supplyscope-assistant
COPILOTKIT_AGENT_NAME=supplyscope-assistant

# Request timeout in milliseconds
# Default: 30000 (30 seconds)
COPILOTKIT_TIMEOUT=30000

# Number of retry attempts on failure
# Default: 3
COPILOTKIT_RETRY_ATTEMPTS=3

# Delay between retries in milliseconds
# Default: 1000 (1 second)
COPILOTKIT_RETRY_DELAY=1000

# -----------------------------------------------------------------------------
# Sentry Error Monitoring (OPTIONAL)
# -----------------------------------------------------------------------------
# Uncomment and configure if using Sentry for error monitoring
# NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn-here
# NEXT_PUBLIC_SENTRY_ORG=your-org
# NEXT_PUBLIC_SENTRY_PROJECT=your-project
# NEXT_PUBLIC_SENTRY_DISABLED=false

# -----------------------------------------------------------------------------
# CopilotKit Cloud (OPTIONAL)
# -----------------------------------------------------------------------------
# Uncomment if using CopilotKit Cloud instead of self-hosted agent
# COPILOT_CLOUD_PUBLIC_API_KEY=ck_pub_your-key-here
# -----------------------------------------------------------------------------
# External API Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Base URL for external API services (e.g., enrichment API)
# If not configured, enrichment features will return an error when used
BASE_API_URL=https://your-api-url.modal.run

# Request timeout for external API calls in milliseconds
# Default: 30000 (30 seconds)
COMPANY_ENRICHMENT_TIMEOUT=30000
